# 设计说明 · Kira ACT 核心体验 0→1

> 一页纸,回答题目两个强制项:AI 反馈什么时候多说、什么时候该闭嘴(附原型例子),以及一处取舍。Persona:现 22、目标 28、距考试 10 周、每天 40 分钟。形态是桌面/Chromebook 优先的响应式 Web——学区里师生用浏览器、设备学校统一发,不做手机 App。核心闭环 6 屏:Onboarding → 自适应诊断 → 个性化计划 → 每日练习加 AI 分层反馈 → 每日回访(Day 1–6)→ 第 7 天里程碑。

## 诊断:在学生失去耐心前拿到画像

不做整套模考,改成 5–10 题的自适应短诊断,精度靠活画像持续校准兜底:
- 用 onboarding 填的现分(22)做初始难度先验;答对上难度、答错下难度,优先补触达最少的域。
- 答满 5 题后给一个诚实的早停点,把取舍摊给学生:"现在停大约 80% 准,再答到 10 题大约 94%"。信号清晰的 5–6 题就走,硬上限 10 题。
- 明确告诉学生:预估分会随每天练习自我校准,不会乱跳、只会更准(复用 Class Atlas 的 per-skill 掌握度和置信度)。这就是"为什么只测这么点"的正面回答。

## AI 反馈:什么时候多说,什么时候闭嘴(强制项)

规则:反馈的多少,取决于学生此刻卡得多深,不取决于我们想教多少。

| 情形 | AI 的动作 | 为什么 |
|---|---|---|
| 第一次就答对 | 一句确认,解析折叠进 "Why (only if you want it)" | 答对还长篇会打断心流、稀释信号,该闭嘴 |
| 第一次答错 | 只给引导式 hint、不给答案,让他再选一次 | 直接给答案会剥夺 productive struggle,自己找出来记得更牢 |
| 提示后答对 | 补一句可迁移的规律 | 他已经会了,固化成规则就行,点到为止 |
| 连错两次 | 才展开:考点名加完整解析加 takeaway | 真卡住时,详解才是帮助、不是噪声 |

两条细化:已经掌握的技能(诊断里答对过的)再答错,走 "probably a slip, look again" 的轻档、不重讲(expertise reversal);连对不空夸,而是 "N in a row, leveling you up"。

反例,修辞题上 AI 反而要收着:语法题答案客观、有规则可引,AI 可以放心讲;修辞题(best opener、add-delete)是判断题,几个选项语法都对、只是"最佳",AI 很容易讲得流畅自信却是错的。所以修辞题先反问目标句("这段到底在讲什么?")让学生先想,再给检索式的克制解析,并显式标注"这是判断题,我这么理解,你有不同看法可以推翻",可举报。原型第 4 屏两道修辞题(`p-en-4`、`p-en-5`)走的就是这套。这正是"该多说"和"该闭嘴"的两面。

## 一处取舍:放弃"进入就做完整模考"(强制项)

放弃的方案是一上来先做整套 ACT 模考、拿最准的画像。不做的原因:对每天只有 40 分钟、还没养成习惯的高中生,第一次就 2–3 小时模考,很可能在养成习惯前就流失,所以诊断准确度让位于留存。代价是短诊断更糙、早停样本更少,用两点对冲:计划侧对信号不足做兜底(英语优先补齐);画像是活的、每天练习持续回流修正,不赌第一天就测准。

## 留存:每日回访 vs 第 7 天里程碑

自学备考最宽的空白就是留存差,教育类 D7 健康线只有约 15–20%。所以回访分两层:Day 1–6 是日常回访,主触发器是间隔复习队列("今天错的 N 张卡,明天回来找你"),把学习机制直接当留存钩子,配 streak 和考试日倒计时;Day 7 做重一点,给可见进步(预估分、掌握度、7 天 streak)加"多数人到不了 Day 7"的正反馈,再加"再练 2 天过一档"的钩子。可变奖励只放激励层,预估分和掌握度保持稳定可信,不被游戏化侵蚀。

## 两个贴合真实考试的判断

- 练习内置节奏时钟:ACT 是限时考试(English 约 42 秒/题),只练对错不练速度,考场上会做不完。所以逐题计时加科目节奏基准,提交后判定是否 on-pace。
- Science / Writing 交给学生选:两科都是选考、不计入 Composite,是否练取决于目标校要求。onboarding 放两个 opt-in 开关(默认关),打开后轻量排入,核心时间仍留给英/数/阅。

技术:Vite + React + TS + Tailwind,纯客户端可点击原型,桌面/Chromebook 优先的两栏布局(`lg` 两栏、`<lg` 单栏)。`npm install && npm run dev`(端口 5180),题目见 `src/data/items.ts`,均为真实 ACT 风格题。
